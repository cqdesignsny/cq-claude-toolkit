---
description: Start-of-session catch-up. Reads the README and handoff doc in the current folder, checks git for drift, posts a short "where we left off" briefing, then waits for direction.
---

Invoke the `startup` skill with the Skill tool before doing anything else, and follow it exactly.

Do not begin any work until the user has responded to the briefing.
